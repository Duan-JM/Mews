HIDE now grows from a circle into a matching row button, then lengthens leftward from a fixed right edge. Past the 20% threshold, the red button springs across the row while only its text snaps to the leading inset. The subtly darker background follows native macOS material and accessibility settings, and settling, retry, and removal animate correctly inside the native session list.
